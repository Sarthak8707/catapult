import express from "express";

const router = express.Router();

// Get all segments
router.get("/", () => {});

// Get info about a segment
router.post("/", () => {});

router.put("/", () => {});

router.delete("/", () => {});