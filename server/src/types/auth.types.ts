export type RegisterInput = {
    username: string,
    password: string,
    email: string
}

export type LoginInput = {
    username: string,
    password: string
}

export type inputToken = {
    id: number,
    username: string
}